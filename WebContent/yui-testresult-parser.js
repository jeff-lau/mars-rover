YUI.add('yui-testresult-parser', function(Y){
	
	var parse = function(result){
		
		for (element in result){
			
		}
		
	};
	
	Y.namespace('TestResultParser').parse = parse;
	
}, '1.0.0', {
	requires: []
});